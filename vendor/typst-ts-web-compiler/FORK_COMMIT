# typst-with-msword-linebreaks b74a09f6d 的 msword 提交 + typst 上游 #792 两个提交（485841043 拆 Space 语法种类、701f12ed5 中日文旁的换行空格丢掉）cherry-pick 到 aa999bec3（typst 0.15.1 基线）之上，再合并到 Myriad-Dreamin/typst tag typst.ts/v0.8.1（fork 主线已并入 typst main，typst.ts 0.8.1 的导出 / world 层编不过 main 的 Format API，站内先走这条 0.15.1 线）
